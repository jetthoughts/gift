<p><%= t('devise.mailer.welcome', name: @resource.name) %></p>

<p><%= t('devise.mailer.unlock_instructions.text') %></p>

<p><%= t('devise.mailer.unlock_instructions.link_label') %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.link'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
