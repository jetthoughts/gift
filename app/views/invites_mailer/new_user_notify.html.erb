<p><%= t('general.invites.hello', name: @invite.name) %></p>

<p><%= t('devise.mailer.invitation_instructions.text', url: root_url) %></p>

<%= render 'project_info', project: @invite.project %>

<p><%= link_to t('devise.mailer.invitation_instructions.link'), new_registration_url(:user, :invite_token => @invite.invite_token) %></p>

<p><%= t('devise.mailer.invitation_instructions.footer') %></p>