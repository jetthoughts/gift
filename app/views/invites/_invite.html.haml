%p
  You was invited to project
    %b= invite.project.name
  %b
  = invite.project.description
  %br
  Created by= invite.admin.name
  %br
  = link_to 'Accept invite', [invite.project, invite], data: { method: :put }
  = link_to 'Ignore invite', [invite.project, invite], data: { confirm: 'Are you sure?', method: :delete }