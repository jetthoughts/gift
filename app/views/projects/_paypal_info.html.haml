= f.simple_fields_for :paid_info, (f.object.paid_info.kind_of?(PayPalInfo) ? f.object : PayPalInfo.new) do |info|
  = info.input :_type, as: :hidden
  = info.input :email, label: false, placeholder: 'Email'